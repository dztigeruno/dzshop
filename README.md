# POTI PATI E-commerce MVP

![POTI PATI](https://via.placeholder.com/800x200/FF6B6B/FFFFFF?text=POTI+PATI+e-commerce)

**منصة تجارة إلكترونية ثنائية اللغة مخصصة للسوق الجزائري**

[![PHP Version](https://img.shields.io/badge/PHP-7.4+-blue.svg)](https://php.net)
[![MySQL](https://img.shields.io/badge/MySQL-5.7+-orange.svg)](https://mysql.com)
[![Bootstrap](https://img.shields.io/badge/Bootstrap-5.3-purple.svg)](https://getbootstrap.com)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

## 📋 المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المميزات](#المميزات)
- [متطلبات النظام](#متطلبات-النظام)
- [التثبيت](#التثبيت)
- [التكوين](#التكوين)
- [الاستخدام](#الاستخدام)
- [API Documentation](#api-documentation)
- [المساهمة](#المساهمة)
- [الرخصة](#الرخصة)
- [الدعم](#الدعم)

## 🎯 نظرة عامة

POTI PATI هو نظام تجارة إلكترونية متكامل مصمم خصيصاً للسوق الجزائري، يدعم اللغتين العربية والفرنسية ويوفر تكامل كامل مع نظام الولايات والبلديات الجزائرية.

### 🎨 التصميم

- **نظام ألوان برتقالي متدرج**: `#FF6B6B` و `#FF8C42` و `#FFD166`
- **تصميم مريح مع زوايا دائرية**
- **تأثيرات ثلاثية الأبعاد**
- **تجاوب كامل مع الأجهزة المحمولة**
- **دعم RTL/LTR**

## ✨ المميزات

### 🌐 **اللغات والترجمة**
- دعم ثنائي اللغة (العربية/الفرنسية)
- تبديل فوري للغة
- دعم كامل للاتجاه من اليمين لليسار (RTL)
- ترجمة تلقائية للعناصر

### 🛒 **التجارة الإلكترونية**
- عرض المنتجات مع الصور المتعددة
- اختيار المتغيرات (اللون، الحجم، التخزين)
- سلة التسوق التفاعلية
- قائمة المفضلة (Wishlist)
- بحث متقدم ومنتجات مقترحة

### 🚚 **المنطق الجزائري**
- **58 ولاية جزائرية** مع بيانات كاملة
- **1541 بلدية** مع التحميل التفاعلي
- حساب التوصيل التلقائي
- مناطق التوصيل المجاني
- أوقات التوصيل المختلفة

### 💳 **نظام الطلبات**
- طلبات بدون تسجيل (Guest Orders)
- دفع عند الاستلام (Cash on Delivery)
- تتبع حالة الطلبات
- إشعارات SMS/Email

### 👨‍💼 **لوحة الإدارة**
- لوحة تحكم شاملة
- إدارة الطلبات والفواتير
- إدارة المنتجات والفئات
- إدارة المخزون
- إحصائيات المبيعات
- تخصيص المظهر

### 📱 **التكامل الاجتماعي**
- **WhatsApp Business** للتواصل المباشر
- مشاركة المنتجات على وسائل التواصل
- تقييمات ومراجعات العملاء
- عروض الخصومات

## 🔧 متطلبات النظام

### الخادم
- **PHP 7.4+** مع الملحقات التالية:
  - PDO MySQL
  - JSON
  - MBString
  - OpenSSL
  - cURL

### قاعدة البيانات
- **MySQL 5.7+** أو **MariaDB 10.3+**

### المتصفح
- Chrome/Firefox/Safari/Edge (الإصدارين الأحدثين)

### الاستضافة
- متوافق مع shared hosting
- XAMPP/WAMP/LAMP للتطوير المحلي

## 🚀 التثبيت

### الطريقة الأولى: التثبيت التلقائي

1. **رفع الملفات**
   ```bash
   # رفع جميع الملفات إلى المجلد الجذر للموقع
   ```

2. **تشغيل المعالج**
   ```
   http://yourdomain.com/install/
   ```

3. **اتباع خطوات التثبيت**
   - فحص متطلبات النظام
   - إعدادات قاعدة البيانات
   - إنشاء حساب المدير
   - تكوين المتجر
   - إتمام التثبيت

### الطريقة الثانية: التثبيت اليدوي

1. **إنشاء قاعدة البيانات**
   ```sql
   CREATE DATABASE poti_pati CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **تعديل ملف الإعدادات**
   ```php
   // ملف includes/config.php
   define("DB_HOST", "localhost");
   define("DB_NAME", "poti_pati");
   define("DB_USERNAME", "your_username");
   define("DB_PASSWORD", "your_password");
   ```

3. **تشغيل SQL**
   ```bash
   # تشغيل ملف database.sql
   mysql -u username -p poti_pati < database.sql
   ```

4. **ضبط الصلاحيات**
   ```bash
   chmod 755 uploads/ cache/ logs/
   ```

## ⚙️ التكوين

### إعدادات قاعدة البيانات

```php
// includes/config.php
define("DB_HOST", "localhost");
define("DB_NAME", "poti_pati");
define("DB_USERNAME", "root");
define("DB_PASSWORD", "");
```

### إعدادات المتجر

```php
define("STORE_NAME", "POTI PATI");
define("STORE_EMAIL", "info@potipati.dz");
define("STORE_PHONE", "+213555123456");
define("DEFAULT_LANGUAGE", "ar");
define("DEFAULT_CURRENCY", "DZD");
```

### إعدادات WhatsApp

```php
define("WHATSAPP_NUMBER", "+213555123456");
define("WHATSAPP_ENABLED", true);
```

### إعدادات التوصيل

```php
define("FREE_SHIPPING_THRESHOLD", 5000); // مجاني للطلبات +5000 دج
define("DEFAULT_SHIPPING_COST", 1000);   // 1000 دج للطلبات الأخرى
```

## 📚 الاستخدام

### للمستخدمين

1. **التسوق**
   - تصفح المنتجات
   - البحث والتصفية
   - إضافة للسلة
   - إتمام الطلب

2. **إدارة الطلب**
   - تتبع الطلب
   - إلغاء الطلب
   - التواصل مع الدعم

### للمديرين

1. **لوحة التحكم**
   ```
   /admin/login.html
   ```

2. **إدارة المنتجات**
   - إضافة منتجات جديدة
   - تعديل المنتجات الموجودة
   - إدارة الصور والمتغيرات
   - تحديث المخزون

3. **إدارة الطلبات**
   - عرض جميع الطلبات
   - تحديث حالة الطلب
   - طباعة الفواتير
   - إحصائيات المبيعات

## 🔌 API Documentation

### Products API

```http
# Get all products
GET /api/products.php?action=list&page=1&limit=12

# Get featured products
GET /api/products.php?action=featured

# Get single product
GET /api/products.php?action=single&id=1

# Search products
GET /api/products.php?action=search&q=smartphone
```

### Orders API

```http
# Create new order
POST /api/orders.php
Content-Type: application/json

{
  "customer_name": "أحمد علي",
  "customer_phone": "+213555123456",
  "delivery_wilaya": "16",
  "delivery_commune": "الجزائر",
  "delivery_address": "حي النصر، شارع الاستقلال",
  "items": [
    {
      "product_id": 1,
      "name": "هاتف ذكي",
      "price": 15999,
      "quantity": 1
    }
  ]
}

# Get orders list
GET /api/orders.php?action=list&status=pending&page=1

# Get single order
GET /api/orders.php?action=single&id=123
```

### Logistics API

```http
# Get all wilayas
GET /api/wilayas.php?action=list

# Get communes by wilaya
GET /api/wilayas.php?action=communes&wilaya_id=16

# Search wilayas/communes
GET /api/wilayas.php?action=search&q=جزائر
```

## 📊 قاعدة البيانات

### الجداول الرئيسية

- **admin** - حسابات المديرين
- **products** - المنتجات
- **categories** - فئات المنتجات
- **orders** - الطلبات
- **order_items** - عناصر الطلبات
- **wilayas** - الولايات الجزائرية
- **communes** - البلديات
- **settings** - إعدادات الموقع

### مثال على استعلام

```sql
-- الحصول على المنتجات المميزة مع الفئات
SELECT p.*, c.name as category_name 
FROM products p 
LEFT JOIN categories c ON p.category_id = c.id 
WHERE p.status = 1 AND p.featured = 1 
ORDER BY p.sales_count DESC 
LIMIT 8;
```

## 🎨 التخصيص

### تغيير الألوان

```css
/* assets/css/style.css */
:root {
  --primary-color: #FF6B6B;
  --secondary-color: #FF8C42;
  --accent-color: #FFD166;
}
```

### إضافة لغة جديدة

1. **إضافة الترجمات في assets/js/main.js**
2. **تحديث ملف اللغة في includes/languages/**
3. **تحديث قواعد CSS للاتجاه**

### تخصيص المظهر

```html
<!-- في admin/theme.html -->
<div class="theme-customizer">
  <input type="color" id="primary-color" value="#FF6B6B">
  <input type="color" id="secondary-color" value="#FF8C42">
  <button onclick="applyTheme()">تطبيق</button>
</div>
```

## 🔒 الأمان

### إجراءات الأمان المطبقة

- **تشفير كلمات المرور** باستخدام password_hash()
- **حماية من SQL Injection** باستخدام Prepared Statements
- **التحقق من البيانات** على الواجهة والخادم
- **تحديد معدل الطلبات** لمنع الإساءة
- **جلسات آمنة** مع انتهاء صلاحية

### توصيات إضافية

```php
// تفعيل HTTPS
define("FORCE_HTTPS", true);

// التحقق من صحة المدخلات
function sanitizeInput($input) {
    return htmlspecialchars(trim($input), ENT_QUOTES, 'UTF-8');
}

// تشفير البيانات الحساسة
function encryptData($data, $key) {
    return openssl_encrypt($data, 'AES-256-CBC', $key, 0, $key);
}
```

## 📈 الأداء

### تحسينات الأداء

- **ضغط الصور** وتقليل حجمها
- **تخزين مؤقت** للاستعلامات الشائعة
- **ضغط الملفات** (Gzip)
- **CDN** للملفات الثابتة

### تحسين قاعدة البيانات

```sql
-- إضافة فهارس لتحسين الأداء
ALTER TABLE products ADD INDEX idx_category (category_id);
ALTER TABLE products ADD INDEX idx_status (status);
ALTER TABLE orders ADD INDEX idx_status (status);
ALTER TABLE orders ADD INDEX idx_created (created_at);
```

## 🧪 الاختبار

### اختبار يدوي

1. **تسجيل مستخدم جديد**
2. **إضافة منتجات للسلة**
3. **إتمام طلب جديد**
4. **إدارة الطلب من لوحة التحكم**

### اختبار برمجي

```bash
# تشغيل اختبارات PHP
phpunit tests/

# اختبار API endpoints
curl -X GET "http://localhost/api/products.php?action=featured"
```

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ في الاتصال بقاعدة البيانات**
   ```php
   // تحقق من إعدادات قاعدة البيانات
   // تأكد من إنشاء قاعدة البيانات
   ```

2. **مشاكل في الصلاحيات**
   ```bash
   chmod 755 uploads/ cache/ logs/
   chmod 644 includes/config.php
   ```

3. **خطأ في تحميل الملفات**
   ```php
   // تحقق من إعدادات PHP
   // upload_max_filesize
   // post_max_size
   ```

### سجلات الأخطاء

```bash
# عرض سجل الأخطاء
tail -f logs/error.log

# سجلات Apache/Nginx
tail -f /var/log/apache2/error.log
tail -f /var/log/nginx/error.log
```

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى اتباع الخطوات التالية:

1. **Fork** المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/amazing-feature`)
3. **Commit** التغييرات (`git commit -m 'Add amazing feature'`)
4. **Push** للفرع (`git push origin feature/amazing-feature`)
5. فتح **Pull Request**

### دليل المساهمة

- اتباع معايير الترميز
- كتابة تعليقات واضحة
- إضافة اختبارات للميزات الجديدة
- تحديث التوثيق

## 📄 الرخصة

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## 📞 الدعم

- **البريد الإلكتروني**: info@potipati.dz
- **هاتف**: +213 555 123 456
- **WhatsApp**: [+213 555 123 456](https://wa.me/213555123456)
- **موقع الويب**: [https://potipati.dz](https://potipati.dz)

## 🙏 شكر وتقدير

- فريق Bootstrap لتصميم رائع
- مجتمع PHP للمكتبة الشاملة
- مطورو Font Awesome للأيقونات
- جميع المساهمين في المشروع

---

**تم تطوير هذا المشروع بـ ❤️ في الجزائر**

*© 2024 POTI PATI. جميع الحقوق محفوظة.*